import React from 'react';
import { ProfilePosts } from './ProfilePosts';

export const Profile = () => {
    return (
        <div>
            Profile page
            <ProfilePosts/>
        </div>
    );
};
